{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/file-upload/macro.njk" import govukFileUpload %}

{% extends 'main_views/_layout.njk' %}
{% block header %}
{% endblock %}

{% block content %}
<div class="full-height">
{% include "partials_views/pageTitle.njk" %}
 <form id="uploadBulk" action="/bulkUpload" method="POST" enctype="multipart/form-data" >

    {{ 
      govukRadios({
      name: "accountType",
      classes: "radio-border govuk-radios--inline",
      fieldset: {
        legend: {
          text: "Select Account Type",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      items: accountType
      }) 
    }}
    <div id="delivery-body-container" class="govuk-form-group">
    {{ 
      govukRadios({
      name: "deliveryBody",
      classes: "radio-border govuk-radios--inline",
      fieldset: {
        legend: {
          text: "Select Delivery Body",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      items: []
      }) 
    }}
    </div>
    
    <div id="invoice-template-body-container" class="govuk-form-group">
    {{ 
      govukRadios({
      name: "invoiceTemplate",
      classes: "radio-border govuk-radios--inline",
      fieldset: {
        legend: {
          text: "Select Scheme Invoice Template",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      items: []
       }) 
    }}
    </div>
    
   {{ govukFileUpload({
     id: "bulk_file",
     name: "bulk_file",
     label: {
      text: "Upload Bulk File"
     }
    }) 
    }}

    {% set errorMessage %}
    Select All The Options
    {% endset %}

    {% include "partials_views/errorBanner.njk" %}
    
    <div class="govuk-button-group">
     {{ govukButton({text: "Save and continue"}) }}
     <a class="govuk-link backButton" href="#">Cancel</a>
    </div>
  
    </form>

</div>
{% endblock %}
{% block footer %}
<script>
    const deliveryBodyData = {{ deliveryBody | dump | safe }};
    const invoiceTemplate = {{ invoiceTemplate | dump | safe }};
    const invoiceTemplateSecondaryData = null;
</script>
{% endblock %}